---
name: host
description: 'Команды на самом сервере: файлы и конфиги, systemd-сервисы и веб-сервер (nginx), firewall (iptables), диски, сеть, git-репозитории проектов; чтение сразу, изменения с подтверждением. Используй, когда нужно посмотреть или поправить конфиг сервиса, проверить и перезагрузить nginx, открыть или закрыть порт, перезапустить системный сервис, узнать состояние репозитория, разобраться с местом на диске или сетью хоста.'
---

## Навык: администрирование хоста

Ты выполняешь команды на самом сервере. Есть два инструмента:
- `host_query` — **только чтение** (cat/head/tail/grep/find/ls/stat, iptables -L/-S,
  df, du, ss, ip show, systemctl status, journalctl, nginx -t/-T, git status/log).
  Безопасен, выполняется сразу.
- `shell_exec` — **любые команды, включая изменяющие**. Опасен, система запросит
  подтверждение пользователя автоматически.

### Общие правила
- Передавай команду списком аргументов: ["iptables", "-L", "-n"], а не строкой.
- Для диагностики/просмотра всегда используй `host_query` — без подтверждения.
- Если `host_query` вернул ошибку «команда не входит в список read-only» —
  значит операция изменяющая: используй `shell_exec`.
- Изменения (добавить правило, restart сервиса и т.п.) — только через `shell_exec`.
- Не выдумывай вывод — сообщай только фактический stdout/stderr и код возврата.

### Плейбук: firewall (iptables)
Порядок безопасной работы с правилами:
1. Осмотр текущих правил: `iptables -L -n -v` и `iptables -S`.
2. Перед изменением зафиксируй, что именно правило делает и не отрежет ли доступ
   (особенно правила на INPUT/22 порт SSH).
3. Добавление: `iptables -A INPUT -p tcp --dport 80 -j ACCEPT`.
4. Удаление по номеру: сначала `iptables -L INPUT --line-numbers`, затем
   `iptables -D INPUT <N>`.
5. Никогда не ставь политику DROP на INPUT, не убедившись, что SSH разрешён явным
   правилом выше по цепочке — иначе потеряешь доступ к серверу.

### Плейбук: systemd
- Статус: `systemctl status <unit>`; логи: `journalctl -u <unit> -n 100 --no-pager`.
- Управление (`start`/`stop`/`restart`) — только по явной задаче пользователя.

### Плейбук: диски
- Использование: `df -h`; крупные каталоги: `du -h -d 1 /path` (без shell glob `*` не раскрывается).

### Плейбук: конфиги и логи сервисов (nginx и др.)
1. Где конфиг: `nginx -T` печатает весь конфиг со всеми include; отдельные файлы —
   `ls -la /etc/nginx/sites-enabled`, `cat <файл>`.
2. Логи: `tail -n 100 /var/log/nginx/error.log`, `grep -c " 502 " /var/log/nginx/access.log`.
3. Правка конфига — через `shell_exec` (с подтверждением). Перед этим сделай копию:
   `cp <файл> <файл>.bak`.
4. После правки обязательно `nginx -t`. Только если проверка прошла —
   `systemctl reload nginx` через `shell_exec`. Не прошла — верни копию и сообщи ошибку.
Так же работай с любым сервисом: найти конфиг, прочитать, поправить с копией,
проверить встроенной командой проверки, перезагрузить.

### Плейбук: git-репозиторий проекта
- Чтение: `git -C /opt/<проект> status -sb`, `git -C /opt/<проект> log --oneline -n 10`,
  что менялось — `git -C /opt/<проект> diff --stat`.
- Содержимое диффа, `git pull`, `git checkout` — через `shell_exec` (с подтверждением).
- Полный деплой проекта (pull + пересборка) — это навык `deploy`, если он выдан.
