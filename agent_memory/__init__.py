"""Память агентской системы: факты, журнал задач, гигиена и консолидация.

Выносимый пакет: внутрь не импортируются ни ядро (`app`), ни библиотека скилов,
ни транспорт (aiogram) и ни клиент LLM — всё внешнее приходит аргументами.
Границу стережёт `tests/test_memory_boundary.py`. Адаптер к системе живёт
в `app/memory/tools.py` и `app/learning/review.py`.
"""
